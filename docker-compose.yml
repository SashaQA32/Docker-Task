version: '3.7'
services:
  db:
    image: postgres:13.7-alpine
    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=db
      - POSTGRES_USER=sashaqa32
      - POSTGRES_PASSWORD=p3Nb1SqXG
